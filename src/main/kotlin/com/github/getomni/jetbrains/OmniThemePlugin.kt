package com.github.getomni.jetbrains

/**
 * Marker so the JetBrains template Kotlin setup compiles. The theme is defined entirely by
 * [Omni.theme.json] and [Omni.xml]; no runtime code is required.
 */
@Suppress("unused")
private object OmniThemePlugin
